<%= form_with(model: type_activity, local: true) do |form| %>
  <% if type_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_activity.errors.count, "error") %> prohibited this type_activity from being saved:</h2>

      <ul>
      <% type_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre_type_activity %>
    <%= form.text_field :nombre_type_activity, id: :type_activity_nombre_type_activity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
