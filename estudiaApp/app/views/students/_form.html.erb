<div id="dialog-form" title="Create new user">
  <p class="validateTips">Selecciona la universidad a la que perteneces </p>
<%= form_with(model: @student, local: true) do |form| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <fieldset>
   <%= form.collection_select(:university_id, University.all, :id, :nombre_university) %>

  <div class="actions">
  <%= form.submit :value => "Guardar Universidad", :class => 'class_name' %>
    <% #form.submit , :style => 'position:absolute; top:-1000px; tabindex: -1;' %>
    </fieldset>
  </div>

  <!--<div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :student_user_id %>
  </div>

  <div class="field">
    <%= form.label :university_id %>
    <%= form.text_field :university_id, id: :student_university_id %>
  </div>-->

  
<% end %>
