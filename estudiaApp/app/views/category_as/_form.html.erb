<%= form_with(model: category_a, local: true) do |form| %>
  <% if category_a.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_a.errors.count, "error") %> prohibited this category_a from being saved:</h2>

      <ul>
      <% category_a.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :category_a_name %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.color_field :color, id: :category_a_color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
