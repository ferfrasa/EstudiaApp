<%= form_with(model: status_activity, local: true) do |form| %>
  <% if status_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_activity.errors.count, "error") %> prohibited this status_activity from being saved:</h2>

      <ul>
      <% status_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre_status_activity %>
    <%= form.text_field :nombre_status_activity, id: :status_activity_nombre_status_activity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
